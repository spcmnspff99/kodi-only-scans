services:
  kodi-scanner:
    build: .
    container_name: kodi-only-scans
    restart: unless-stopped
    ports:
      - "8282:8282"
    env_file:
      - .env
    environment:
      TZ: America/Boise
    volumes:
      - scanner-data:/data
    # Optional: run an initial scan immediately on startup
    # (remove if you prefer cron-only or manual triggers)
    # command: >
    #   sh -c "uvicorn main:app --host 0.0.0.0 --port 8282 &
    #          sleep 10 && python -c 'from main import run_scan; run_scan()' &&
    #          wait"

volumes:
  scanner-data:
    driver: local
